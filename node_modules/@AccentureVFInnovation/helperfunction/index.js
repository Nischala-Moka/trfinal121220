// const add = (a: number, b: number): number => {
//   return a + b;
// };

const subtract = function (a, b) {
  console.log(`Subtraction of values ${a}&${b} are : ${a - b}`);
  return `Subtraction of values ${a}&${b} are : ${a - b}`;
};

module.exports = { subtract };
